Build & upload arm_controller via PlatformIO. ESP will be AP ESP32_AP at 192.168.4.1 by default.
Run controller.py from repo root while PC is connected to that AP. The controller will map poses to 4 servos: base, shoulder, elbow, claw.

